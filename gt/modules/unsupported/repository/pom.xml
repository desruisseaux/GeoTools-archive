<?xml version="1.0" encoding="UTF-8"?>


<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  
  
  
  <modelVersion>4.0.0</modelVersion>

  <parent>
     <groupId>org.geotools</groupId>
     <artifactId>unsupported</artifactId>
     <version>2.5-SNAPSHOT</version>
  </parent>

  <groupId>org.geotools</groupId>
  <artifactId>repository</artifactId>
  <packaging>jar</packaging>
  <version>2.5-SNAPSHOT</version>
  <name>Repository</name>
  <url>http://maven.geotools.fr/reports/modules/unsupported/repository</url>

  <scm>
    <connection>
      scm:svn:http://svn.geotools.org/geotools/trunk/gt/modules/unsupported/repository
    </connection>
    <url>http://svn.geotools.org/geotools/trunk/gt/modules/unsupported/repository</url>
  </scm>

  <description>
  A repository implementation used to manage GeoResources. The focus is on maintaining a single DataStore
  or GridCoverage reader for each service. This work was origionally ported from the uDig catalog into
  the main geotools library.
  </description>

  <licenses>
    <license>
      <name>Lesser General Public License (LGPL)</name>
      <url>http://www.gnu.org/copyleft/lesser.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
  	<developer>
  		<id>jive</id>
  		<name>Jody Garnett</name>
  		<roles>
  			<role>Module Maintainer</role>
  		</roles>
  		<organization>Refractions Research Inc</organization>
  		<organizationUrl>http://www.refractions.net/</organizationUrl>
  		<email>jgarnett@refractions.com</email>
  		<timezone>PST</timezone>
        </developer>
  </developers>

  <dependencies>
    <dependency>
      <groupId>org.geotools</groupId>
      <artifactId>main</artifactId>
      <version>2.5-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>org.geotools</groupId>
      <artifactId>postgis</artifactId>
      <version>2.5-SNAPSHOT</version>
    </dependency>    
    <dependency>
      <groupId>org.geotools</groupId>
      <artifactId>wms</artifactId>
      <version>2.5-SNAPSHOT</version>
    </dependency>    
    <dependency>
      <groupId>org.geotools</groupId>
      <artifactId>wfs</artifactId>
      <version>2.5-SNAPSHOT</version>
    </dependency>   
    <dependency>
      <groupId>org.geotools</groupId>
      <artifactId>render</artifactId>
      <version>2.5-SNAPSHOT</version>
    </dependency>    
    <dependency>
      <groupId>org.geotools</groupId>
      <artifactId>shapefile</artifactId>
      <version>2.5-SNAPSHOT</version>
    </dependency>
    <!-- Test dependencies -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>3.8.1</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <!-- =========================================================== -->
  <!--     Build Configuration                                     -->
  <!-- =========================================================== -->
  <build>
    <plugins>
      <!-- ====    Code Formatting  ============================== -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>jalopy-maven-plugin</artifactId>
        <version>1.0-SNAPSHOT</version>
        <configuration>
            <convention>resources/jalopy-geotools.xml</convention>
        </configuration>
        <executions>
          <execution>
            <goals>
              <!-- <goal>format</goal> -->
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
